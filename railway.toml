[build]
builder = "NIXPACKS"
buildCommand = "./mvnw clean install -DskipTests"

[deploy]
startCommand = "java -jar target/servicios-0.0.1-SNAPSHOT.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
